# Descrições manuais / Hand-written notes

Crie `<id>.pt.md` e/ou `<id>.en.md` (o `id` é o do item, ex.: `raw8`) com dicas de **como obter**
o item. O texto aparece na página do item junto com a descrição do jogo. Markdown comum.

Create `<id>.en.md` and/or `<id>.pt.md` (e.g. `raw8`) with tips on **how to obtain** the item.
